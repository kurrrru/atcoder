#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main()
{
	ll N, M, C, K;
	cin >> N >> M >> C >> K;
	vector<ll> A(N);
	for (ll i = 0; i < N; i++)
	{
		cin >> A[i];
		A[i] %= M;
	}
	sort(A.begin(), A.end());
	ll ans = 0;
	for (ll k = 0; k < K; i++)
	{
		ll p = 
	}
}
